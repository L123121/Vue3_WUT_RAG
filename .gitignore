# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files
.env
backend/.env
deploy/.env.production

# Session/cookie files
cookies.txt
# 教务系统抓取脚本产生的真实凭证与数据（含真实 cookie/成绩/课表，禁止提交）
scripts/jw-cookies.json
scripts/grade-data.json
scripts/schedule-data.json
scripts/grade-page-data.json
scripts/grade-api.json

# Design archive (old code)
src/design-archive/

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Runtime / cache / volumes
.model-cache/
volumes/
__pycache__/
.claude/worktrees/
scripts/rag-eval/results/
backend/scripts/rag-eval/results/

# Lighthouse / 性能审计产物（报告由 CI 作为 Artifacts 输出，不入库）
.lighthouseci/
lighthouse*.json
backend/lighthouse*.json

# 一次性性能 benchmark 调试文件
benchmark-fps.*
public/benchmark-fps.*

# 本地工具链配置（含历史真实凭证，禁止提交）
.claude/settings.local.json

# Eval cookies 与临时探针文件（含凭据，禁止提交）
.eval-cookie*.txt
scripts/rag-eval/.admin-cookie.*
scripts/rag-eval/.plain.txt
scripts/rag-eval/.probe-*.txt
scripts/rag-eval/.retr.txt
scripts/rag-eval/.trace*.txt

# 大二进制产物不入库
arrow.tar.gz

# 本地 agent 工作目录（规划/便签等过程文档，不公开）
.claude/
.workbuddy/
backend/src/data/
